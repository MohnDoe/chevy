services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile.bot
    restart: always
    environment:
      - NODE_ENV=${NODE_ENV}
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      - BOT_ON_HEVY_AUTH_TOKEN=${BOT_ON_HEVY_AUTH_TOKEN}
      - BOT_ON_HEVY_USERNAME=${BOT_ON_HEVY_USERNAME}
      - DATABASE_URL=${DATABASE_URL}
      - POSTHOG_API_KEY=${POSTHOG_API_KEY}
      - POSTHOG_HOST=${POSTHOG_HOST}
      - CHEVY_LIVE_ACTIVITY_WEBHOOK_URL=${CHEVY_LIVE_ACTIVITY_WEBHOOK_URL}
      - CHEVY_AUTO_SHARE_TASK_SCHEDULE=${CHEVY_AUTO_SHARE_TASK_SCHEDULE}
      - CHEVY_BULLMQ_REDIS_HOST=${CHEVY_BULLMQ_REDIS_HOST}
      - CHEVY_VERIFICATION_TASK_SCHEDULE=${CHEVY_VERIFICATION_TASK_SCHEDULE}
      - CHEVY_VERIFICATION_CODE_LENGTH=${CHEVY_VERIFICATION_CODE_LENGTH}
      - CHEVY_VERIFICATION_WORKOUT_SHORT_ID=${CHEVY_VERIFICATION_WORKOUT_SHORT_ID}
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
