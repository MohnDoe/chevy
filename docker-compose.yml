services:
  bot:
    build:
      context: .
      dockerfile: ./apps/bot/Dockerfile
      args:
        ENV_VARIABLE: ${ENV_VARIABLE}
    restart: always
    env_file: .env
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
